---
phase: 03-mythological-narrative-encounters
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/data/encounters.js
  - src/systems/encounter.js
  - src/scenes/encounterScene.js
  - .planning/phases/03-mythological-narrative-encounters/03-UAT.md
autonomous: false
requirements:
  - REQ-003
  - REQ-004
  - REQ-005
must_haves:
  truths:
    - "At least 8 lore-grounded encounters exist across devas, asuras, rishis, yakshas, and nagas — respectful tone"
    - "A later encounter branches on a marker written by an earlier encounter (REQ-004 chain)"
    - "Encounters can be gated by karma (high-karma blessing vs low-karma temptation) and by choice-level flags"
    - "Item rewards flow through Economy.addItem with quest tracking; all phase rewards route only through canonical APIs"
    - "Markers and the completion ledger survive save/load (proven in browser UAT)"
  artifacts:
    - path: "src/data/encounters.js"
      provides: "8 total ENCOUNTERS entries (5 zone + 3 travel) with karma bounds and marker chains"
      contains: "asuraWhisper"
    - path: "src/systems/encounter.js"
      provides: "choice.flagsReq filtering in getChoices + guard in choose"
      contains: "choice.flagsReq"
    - path: "src/scenes/encounterScene.js"
      provides: "renders only EncounterSystem.getChoices(id) (system-filtered choices)"
      contains: "getChoices"
    - path: ".planning/phases/03-mythological-narrative-encounters/03-UAT.md"
      provides: "Browser UAT evidence: trigger → branch → persistence"
      contains: "status: complete"
  key_links:
    - from: "src/data/encounters.js"
      to: "src/systems/encounter.js"
      via: "choice.flagsReq read in getChoices/choose"
      pattern: "flagsReq"
    - from: "src/data/encounters.js"
      to: "G.state.flags"
      via: "enc_nagaBargain marker chain → nagaElder choice gating"
      pattern: "enc_nagaBargain"
    - from: "src/systems/encounter.js"
      to: "Economy.addItem"
      via: "herb rewards"
      pattern: "Economy\\.addItem"
---
<objective>
Complete the encounter library (6-10 lore-grounded encounters) with karma gates and choice-level flag branching, then prove the full phase with automated source assertions and a browser UAT.
Purpose: Satisfy REQ-003 (lore breadth + respectful tone), REQ-004 (choices echo forward — marker chains across encounters), and REQ-005 (canonical reward routing) with verifiable evidence.
Output: 8 total encounters; choice-level `flagsReq` support; phase-wide verification sweep; 03-UAT.md with results.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mythological-narrative-encounters/03-RESEARCH.md
@.planning/phases/03-mythological-narrative-encounters/03-01-SUMMARY.md
@.planning/phases/03-mythological-narrative-encounters/03-02-SUMMARY.md

Content follows existing lore conventions: `src/data/journeys.js` (mythology-grounded choices with karma/gold/stat rewards), `src/data/zones.js` (karma in zone completion rewards). The marker chain pattern (encounter A writes `G.state.flags['enc_<id>']`, encounter B reads it) mirrors `getAvailableJourneys` flag/prereq filtering and the `boss_svarga` flag precedent in trials.js/combatScene.js.

<interfaces>
<!-- Contracts built on from plans 03-01/03-02. Only the additions below are NEW this plan: -->
```js
// 1. choice-level gating (NEW):
// EncounterSystem.getChoices(id) → returns enc.choices FILTERED to choices whose
//    choice.flagsReq (if present) is fully satisfied by G.state.flags. Hidden choices
//    are simply omitted — the scene renders whatever getChoices returns.
// EncounterSystem.choose(id, choiceIdx) → choiceIdx must ALSO satisfy the flag gate
//    (defense-in-depth: a console caller cannot select a gated choice out of band).
// choice.flagsReq semantics are IDENTICAL to encounter-level flagsReq:
//    every [k,v] must satisfy (G.state.flags && G.state.flags[k]) === v, where v===true means present-and-truthy.

// 2. Reward keys allowed (unchanged from 03-01): gold, karma, xp, prana, cultivationBase,
//    divineFragments, items[{name,type,qty}], hp, str, agi, mag, def. choice.karma (post-reward delta).

// 3. Encounter roster after this plan (8 total):
//    ZONE: nagaBargain (dandaka), yakshaGrove (aryavarta), nagaElder (patala),
//          rishiBoon (meru, karmaMin 10), asuraWhisper (patala, karmaMax 5)
//    TRAVEL: marutCrossing, devaGandharva, apsaraMirror
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Expand ENCOUNTERS to 8 lore encounters + choice-level flagsReq support</name>
  <files>src/data/encounters.js, src/systems/encounter.js, src/scenes/encounterScene.js</files>
  <action>
1. `src/data/encounters.js` — ADD six encounters to `ENCOUNTERS` (keep the two seeds). Exact specs:
   - `yakshaGrove` — pool 'zone', zones ['aryavarta'], weight 10, lvlMin 1, lvlMax 99, flagsReq {}. Prompt: a yaksha guards a grove of flowering herbs and offers to share its bounty for a gift. Choices:
     * { text: 'Offer gold', preview: 'Lose 30 gold; the yaksha gifts you healing herbs', reward: { gold: -30, karma: 3, items: [ { name: 'Healing Herb', type: 'herb', qty: 2 } ] }, flags: { 'enc_yakshaGrove': 'offer' } }
     * { text: 'Take what you need', preview: 'Take herbs and gold by force', reward: { gold: 50, karma: -1 }, flags: { 'enc_yakshaGrove': 'take' } }
   - `nagaElder` — pool 'zone', zones ['patala'], weight 10, lvlMin 1, lvlMax 99, flagsReq {}. Prompt: an ancient naga elder coils before the gates of Patala and remembers a traveler who met his kin in Dandaka. Choices:
     * { text: 'Ask after her blessing', preview: 'Prana and karma', reward: { prana: 80, karma: 2 }, flags: { 'enc_nagaElder': 'blessing' } }
     * { text: 'Offer tribute', preview: 'Lose 40 gold, gain experience', reward: { gold: -40, xp: 120 }, flags: { 'enc_nagaElder': 'tribute' } }
     * { text: 'Pray for mercy', preview: 'He remembers your mercy in Dandaka...', reward: { karma: 4, prana: 120 }, flags: { 'enc_nagaElder': 'mercy' }, flagsReq: { 'enc_nagaBargain': 'share' } }   ← REQ-004 chain
   - `rishiBoon` — pool 'zone', zones ['meru'], weight 10, lvlMin 10, lvlMax 99, karmaMin 10, karmaMax null, flagsReq {}. Prompt: a rishi beneath a tree of stars offers one boon. Choices:
     * { text: 'Seek wisdom', preview: 'Cultivation base and prana', reward: { cultivationBase: 15, prana: 60 }, flags: { 'enc_rishiBoon': 'wisdom' } }
     * { text: 'Seek strength', preview: 'Strength and experience', reward: { str: 1, xp: 150 }, flags: { 'enc_rishiBoon': 'strength' } }
   - `asuraWhisper` — pool 'zone', zones ['patala'], weight 10, lvlMin 5, lvlMax 99, karmaMin null, karmaMax 5, flagsReq {}. Prompt: a bound asura prince in the dark offers a reward for release. Choices:
     * { text: 'Refuse the prisoner', preview: 'Karma and experience', reward: { karma: 3, xp: 60 }, flags: { 'enc_asuraWhisper': 'refuse' } }
     * { text: 'Sever the chains', preview: 'Great gold and a divine fragment, at karma's cost', reward: { gold: 150, divineFragments: 1, karma: -3 }, flags: { 'enc_asuraWhisper': 'free' } }
   - `devaGandharva` — pool 'travel', zones ['aryavarta','dandaka','meru'], weight 10, lvlMin 1, lvlMax 99, flagsReq {}. Prompt: a gandharva's song weaves the wind into melody along the road. Choices:
     * { text: 'Learn the verse', preview: 'Prana and experience', reward: { prana: 40, xp: 80 }, flags: { 'enc_gandharva': 'verse' } }
     * { text: 'Ask for a blessing', preview: 'Gold and karma', reward: { gold: 60, karma: 1 }, flags: { 'enc_gandharva': 'blessing' } }
   - `apsaraMirror` — pool 'travel', zones ['dandaka','meru','patala'], weight 10, lvlMin 1, lvlMax 99, flagsReq {}. Prompt: an apsara holds up a mirror of water showing a life of comfort and forgetting. Choices:
     * { text: 'Look past the mirror', preview: 'Karma and experience', reward: { karma: 3, xp: 50 }, flags: { 'enc_apsaraMirror': 'lookpast' } }
     * { text: 'Reach for the vision', preview: 'Gold, at karma's cost', reward: { gold: 100, karma: -1 }, flags: { 'enc_apsaraMirror': 'reach' } }
   Tone check: devas/gandharvas/apsaras/rishis appear benevolent or testing; the asura is a tempting figure, never demeaned (REQ-003).
2. `src/systems/encounter.js` — add choice-level gating:
   - `getChoices(id)`: currently returns raw choices; change to filter: `const enc = ENCOUNTERS[id]; if (!enc || !enc.choices) return []; return enc.choices.filter(ch => this._choiceVisible(ch));` where `_choiceVisible(ch)` returns true when `!ch.flagsReq` or every [k,v] of Object.entries(ch.flagsReq) satisfies `(G.state.flags && G.state.flags[k]) === v`
   - `choose(id, choiceIdx)`: after resolving `const choice = enc.choices && enc.choices[choiceIdx]` add `if (!choice || !this._choiceVisible(choice)) return false;` (defense-in-depth)
3. `src/scenes/encounterScene.js` — `buildUI(enc)`: replace the direct `enc.choices[i]` iteration with `const choices = EncounterSystem.getChoices(enc.id)` and iterate THAT array, indexing `choose(encounterScene.data.encounterId, i)` by the same array position. The scene holds NO flag logic — single source of truth is the system.
4. Do not change reward routing or ledger semantics. Do not touch trigger wiring.
  </action>
  <verify>
    <automated>grep -c "const ENCOUNTERS" src/data/encounters.js && node -e "const s=require('fs').readFileSync('src/data/encounters.js','utf8'); const ids=['nagaBargain','yakshaGrove','nagaElder','rishiBoon','asuraWhisper','marutCrossing','devaGandharva','apsaraMirror']; const missing=ids.filter(id=>!s.includes(id+':')); if(missing.length){console.error('missing:',missing);process.exit(1)}; if(!s.includes('karmaMin: 10')){console.error('rishiBoon karma gate missing');process.exit(1)}; if(!s.includes('karmaMax: 5')){console.error('asuraWhisper karma gate missing');process.exit(1)}; if(!s.includes(\"'enc_nagaBargain': 'share'\")){console.error('chain marker missing');process.exit(1)}; console.log('roster OK')" && grep -c "_choiceVisible" src/systems/encounter.js && grep -c "getChoices" src/scenes/encounterScene.js</automated>
  </verify>
  <done>ENCOUNTERS contains exactly 8 entries with the specified pools/gates/chain; getChoices filters by choice.flagsReq; choose re-validates visibility; scene renders only system-filtered choices; seeds untouched except registry growth</done>
</task>

<task type="auto">
  <name>task 2: Phase-wide automated verification sweep (source assertions + deterministic browser checks)</name>
  <files>src/data/encounters.js, src/systems/encounter.js, src/scenes/encounterScene.js, src/scenes/zoneExploration.js, src/scenes/travelMap.js</files>
  <action>
Run the full verification sweep and record results into `.planning/phases/03-mythological-narrative-encounters/03-UAT.md` (structure it like 02-UAT.md: `status: complete|incomplete`, phase, source, `## Tests` with numbered expected/result/pass-fail entries). Entries:
1. Source assertions (run all, record pass/fail):
   a. `grep -c "const ENCOUNTERS" src/data/encounters.js` === 1
   b. Node roster check (task-1 verify command) passes: 8 ids present, karmaMin/karmaMax gates, chain marker
   c. Node canonical-routing check on src/systems/encounter.js: no direct `G.state.gold|karma|divineFragments|prana|cultivationBase` assignment; contains Economy.addGold, Economy.addKarma, Economy.addDivineFragments, Progression.addPartyXP, CultivationSystem.addPrana, CultivationSystem.addCultivationBase, Economy.addItem, QuestSystem.trackCollect, AchievementSystem.check
   d. Node reward-key whitelist check on src/data/encounters.js: every `reward:` object's keys ∈ {gold,karma,xp,prana,cultivationBase,divineFragments,items,hp,str,agi,mag,def} and every `choice.karma` is numeric; every flags key starts with 'enc_'
   e. `grep -c "EncounterTrigger.rollZone" src/scenes/zoneExploration.js` === 1; `grep -c "EncounterTrigger.rollTravel" src/scenes/travelMap.js` === 1; both gScene('encounterScene') routes present
   f. `grep -c "getChoices" src/scenes/encounterScene.js` >= 1 (scene uses system-filtered choices)
2. Deterministic browser checks (via your CDP automation — the repo pattern from Phase 02; open the game, use console freezes: `EncounterTrigger.NARRATIVE_CHANCE = 1` for determinism, restore 0.3 after): use a scratch save (manual `SaveSystem.save()` snapshot before, restore after, or a fresh profile) so the player's real save is untouched:
   a. Zone trigger + rewards: enter dandaka zoneExploration → encounterScene appears with nagaBargain (force roll) → choose 'Share your gold' → result panel lists grants; console: gold decreased by 50 (clamped), karma +3, party XP +60; `G.state.flags['enc_nagaBargain'] === 0`; `G.state.encounters.seen.nagaBargain === 0`
   b. No re-trigger: force roll again in dandaka → no encounterScene for nagaBargain (other zoned encounters may roll; assert nagaBargain absent)
   c. Persistence: `SaveSystem.save()` → reload page → `EncounterSystem.isSeen('nagaBargain')` true, flag intact, karma/gold deltas intact
   d. REQ-004 chain: set `G.state.flags['enc_nagaBargain'] = 'share'` via `EncounterSystem.setFlag` (as if shared) → unlock patala → force-roll → nagaElder shows 3 choices including 'Pray for mercy'; set marker to 'force' → nagaElder shows exactly 2 choices (gated choice hidden); choose 'Pray for mercy' → prana +120, karma +4
   e. Karma gating: set `G.state.karma = 12` → force-roll in meru → rishiBoon appears; `G.state.karma = 2` → rishiBoon does NOT appear (rolls another meru encounter or none)
   f. Item routing: choose yakshaGrove 'Offer gold' → inventory includes Healing Herb qty +2 (stacked); no crash from QuestSystem.trackCollect
   g. Back-out safety: force roll → tap 'Leave' before choosing → back in zoneExploration; `isSeen` false; no flags written
   h. Travel lane: with NARRATIVE_CHANCE 1, travel dandaka→meru → travel encounter appears (marutCrossing/devaGandharva/apsaraMirror per zone list) → choose → arrives in meru (currentZone === 'meru', zoneExploration active)
3. Record EVERY check in 03-UAT.md with `expected`, `result`, `pass|fail`. Set `status: complete` ONLY if all checks pass.
4. If any check fails, fix the defect (single source of truth remains EncounterSystem/data — scene holds no logic), re-run the sweep, and record the fix in the UAT notes section.
  </action>
  <verify>
    <automated>grep -c "status: complete" .planning/phases/03-mythological-narrative-encounters/03-UAT.md</automated>
  </verify>
  <done>03-UAT.md written with status complete and every sweep item from the list recorded as pass; scratch-save approach left the real save untouched</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 3: Browser UAT — player-facing encounter experience</name>
  <files>.planning/phases/03-mythological-narrative-encounters/03-UAT.md</files>
  <action>Automated sweep from task 2 already ran — the checkpoint confirms the human-visible experience on a REAL save flow (OpenCode assists by driving console commands / CDP; the user watches and approves).</action>
  <verify>All four flows below behave correctly in the browser; 03-UAT.md updated with the human-verification results</verify>
  <done>User types "approved" (or describes issues) confirming the encounter experience</done>
  <what-built>
Phase 03 myth narrative encounters:
- `src/data/encounters.js` — 8 encounters (5 zone, 3 travel): nagaBargain, yakshaGrove, nagaElder, rishiBoon, asuraWhisper, marutCrossing, devaGandharva, apsaraMirror — with karma gates (rishiBoon karmaMin 10, asuraWhisper karmaMax 5), marker chains (nagaElder reacts to enc_nagaBargain), canonical rewards
- `src/systems/encounter.js` — EncounterSystem (start/choose/setFlag/ledger) + EncounterTrigger (zone/travel rolls) with choice-level flagsReq filtering
- `src/scenes/encounterScene.js` — choice UI with prompt, previews, result panel, origin resume
- Trigger wiring in `zoneExploration.js` (narrative roll on enemy timer) and `travelMap.js` (travel-lane roll)
- Save integration: `G.state.encounters` default + migrate normalization; markers in `G.state.flags['enc_*']`
  </what-built>
  <how-to-verify>
Open `index.html` in a browser (use a SCRATCH save/profile — do not consume the real save):
1. **Zone encounter** — enter Dandaka Forest from the travel map and watch the explorer: within a few encounter cycles a narrative prompt appears (naga or another lore figure). Read the prompt and the two choice cards — consequences are previewed on each card.
2. **Choice + reward** — pick a choice. A result panel appears listing what you gained (gold/xp/karma/item). Press Continue — you return to zone exploration, which keeps running. Check the console: `G.state.flags['enc_<id>']` is set and `G.state.encounters.seen.<id>` is marked.
3. **Persistence** — reload the page and return to the same zone: the resolved encounter does NOT re-trigger; flag/marker survive; rewards were not re-granted.
4. **Travel encounter** — travel to Mount Meru: about a third of trips surface a travel-lane encounter (gandharva song, apsara mirror, or Maruts) before arrival; choosing Continue lands you in Meru with exploration active. Backing out with 'Leave' returns you without side effects.
5. **Karma variety** — with high karma (console `Economy.addKarma(15)`), the Meru rishi boon can appear; with very low karma (set `G.state.karma = 2`), the Patala asura whisper can tempt instead — demonstrating karma-driven spawns.
Report any visual glitches, wrapped-text overflow, or flows that break; otherwise type "approved".
  </how-to-verify>
  <resume-signal>"approved" or a description of the issues found</resume-signal>
</task>

</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Authored encounter content → runtime state | Content is static and trusted (repo-authored), but reward keys/values must stay inside the whitelist so a bad edit cannot create new mutation paths |
| Console caller → EncounterSystem.choose | Console can call choose directly; choice visibility must be re-validated at the system layer (not only hidden in UI) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | choice.flagsReq bypass via console | mitigate | choose() re-runs _choiceVisible on the actual choice object; hidden choices return false |
| T-03-10 | Tampering | reward data drift | mitigate | automated whitelist check (task 2, item d) asserts every reward key ∈ allowed set; flags keys prefixed 'enc_' |
| T-03-11 | Tampering | marker replay to re-claim rewards | mitigate | seen-ledger guard in choose() (unlocked only on resolution) + UAT persistence check (reload does not re-grant) |
| T-03-12 | Elevation | item grant side channel | mitigate | items route exclusively through Economy.addItem (stacking + QuestSystem.trackCollect); no inventory.push in new files (grep-gated) |
</threat_model>
<verification>
- Task-1 automated gates: roster (8 ids), karma gates (karmaMin 10 / karmaMax 5), chain marker enc_nagaBargain → 'share', _choiceVisible present, scene uses getChoices
- Task-2 sweep fully recorded in 03-UAT.md with status complete: source assertions a-f AND deterministic browser checks a-h (trigger/rewards/no-retrigger/persistence/chain/karma-item/back-out/travel)
- Task-3 human UAT approved on a scratch save
</verification>
<success_criteria>
- 8 encounters exist, respectful and myth-grounded, covering devas/asuras/rishis/yakshas/nagas (REQ-003)
- nagaElder visibly branches on the enc_nagaBargain marker; rishiBoon and asuraWhisper gate on karma (REQ-004)
- Every reward routes through canonical APIs — source assertion + UAT evidence (REQ-005)
- Markers and ledger survive save/load; no re-trigger, no re-grant (UAT items c, b)
- 03-UAT.md committed with status complete
</success_criteria>
<output>
After completion, create `.planning/phases/03-mythological-narrative-encounters/03-03-SUMMARY.md`
</output>
