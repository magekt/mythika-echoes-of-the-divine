---
phase: 03-mythological-narrative-encounters
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/scenes/encounterScene.js
  - src/scenes/zoneExploration.js
  - src/scenes/travelMap.js
  - index.html
autonomous: true
requirements:
  - REQ-003
must_haves:
  truths:
    - "Exploring a zone can surface a narrative encounter instead of an enemy spawn"
    - "Traveling to a new zone can surface a travel-lane encounter before arrival"
    - "The encounter screen shows prompt text and at least two choice cards with consequence previews"
    - "Choosing a card immediately resolves rewards and returns to the correct origin (resumes exploration or completes travel)"
    - "Leaving/backing out of the encounter screen without choosing does not mark it seen (no reward, no flag, no ledger write)"
  artifacts:
    - path: "src/scenes/encounterScene.js"
      provides: "Choice UI (prompt, choice cards, result panel, continue)"
      exports: ["encounterScene"]
    - path: "src/scenes/zoneExploration.js"
      provides: "Narrative roll on enemy timer firing"
      contains: "EncounterTrigger.rollZone"
    - path: "src/scenes/travelMap.js"
      provides: "Travel-lane narrative roll before ZoneAccess.enter"
      contains: "EncounterTrigger.rollTravel"
  key_links:
    - from: "src/scenes/zoneExploration.js"
      to: "encounterScene"
      via: "gScene('encounterScene', true, { encounterId, origin })"
      pattern: "gScene\\('encounterScene'"
    - from: "src/scenes/travelMap.js"
      to: "encounterScene"
      via: "gScene('encounterScene', true, { encounterId, origin, pendingZone })"
      pattern: "pendingZone"
    - from: "src/scenes/encounterScene.js"
      to: "EncounterSystem.choose"
      via: "choice card onClick"
      pattern: "EncounterSystem\\.choose"
    - from: "src/scenes/encounterScene.js"
      to: "ZoneAccess.enter"
      via: "travel origin resume"
      pattern: "ZoneAccess\\.enter"
---
<objective>
Deliver the encounter user interface and wire the two trigger points so encounters actually surface during play.
Purpose: Close the player-facing loop of REQ-003 — encounters appear in exploration/travel, choices are visible, and resolution returns the player seamlessly to where they were.
Output: `encounterScene` (choice UI on Canvas), narrative roll in `zoneExploration` enemy timer, travel-lane roll in `travelMap`, resume logic for both origins.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mythological-narrative-encounters/03-RESEARCH.md
@.planning/phases/03-mythological-narrative-encounters/03-01-SUMMARY.md

Follow the existing scene patterns:
- `gScene(name, fade, enterOptions)` → `scene.enter(enterOptions)` (game.js line 449). `party.js` line 12 shows scenes receiving args in `enter()` — `enter: function(heroId)`.
- `journeyScene` (src/scenes/journeyScene.js) — closest UI analog: `Scene.create`, `data.buttons`, `UI.PremiumShell` header cards, custom `btn.render` overriding with accent bars, `journeyTextLines` text wrapping
- Scene scroll conventions (src/engine/scene-helpers.js): `Scene.scrollInput(scene)` on `scroll`, `clampScroll()`, `Scene.drawScrollbar(ctx, top, contentHeight, viewportH, scrollY, minBar)` in render, `Scene.cullButtons(buttons, scrollY, viewportH)` before iterating buttons, `Scene.drawStatic(ctx, scene, ...)` + `ctx.translate(0, -scrollY)` for scrolled content
- `zoneExploration.js` lines 264-273 — the enemy-timer block the narrative roll hooks into
- `travelMap.js` lines 190-200 — the travel action the travel-lane roll hooks into
- `R.reducedMotion()` check before any animation; `R.colors.*` tokens; `R.fonts.sm/md`; `R.radius.s/m`; `Notify.show(text, duration, color)`

<interfaces>
<!-- Contracts consumed from plan 03-01 (available after that plan executes): -->
```js
const ENCOUNTERS = { /* id → entry { id, name, icon, pool, zones, weight, lvlMin, lvlMax, karmaMin, karmaMax, flagsReq, prompt, choices[] } */ };
const EncounterSystem = {
  start: function(id) {},          // → encounter object | null (null when unknown or already seen)
  getChoices: function(id) {},     // → choices[] | []
  choose: function(id, choiceIdx) {} // → { id, choiceIdx, text, granted[], marker } | false
};
const EncounterTrigger = {
  NARRATIVE_CHANCE: 0.3,
  rollZone: function(zoneId) {},      // → encounter id | null (PURE)
  rollTravel: function(fromZone, toZone) {} // → encounter id | null (PURE)
};
// scene navigation: gScene('encounterScene', true, { encounterId, origin, pendingZone })
// origin: 'zoneExploration' | 'travelMap'
// resume: zoneExploration → gScene('zoneExploration', true); travelMap + pendingZone → ZoneAccess.enter(pendingZone) then gScene('zoneExploration', true); travelMap without pendingZone → gScene('travelMap', true)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Create encounterScene — prompt, choice cards, result panel, origin resume</name>
  <files>src/scenes/encounterScene.js, index.html</files>
  <action>
Create `src/scenes/encounterScene.js`:
1. Copy the text-wrapping helper from journeyScene.js (`journeyTextLines` + `journeyFitText`) as local functions `encounterTextLines` / `encounterFitText` (same implementation; the helper is scene-local in journeyScene, so duplicate it — do not import across scenes).
2. `const encounterScene = Scene.create({ name: 'encounterScene', ... })` with `data: { buttons: [], scrollY: 0, contentHeight: 0, staticDraws: [], encounterId: null, origin: null, pendingZone: null, resolved: false, result: null }`.
3. `enter: function(opts) { ... }`:
   - `opts = opts || {}`; `this.data.encounterId = opts.encounterId || null`; `this.data.origin = opts.origin || 'zoneExploration'`; `this.data.pendingZone = opts.pendingZone || null`; `this.data.resolved = false`; `this.data.result = null`; `this.data.scrollY = 0`
   - `const enc = EncounterSystem.start(this.data.encounterId)`; defensive: if `!enc` → `Notify.show('The vision fades...', 2, R.colors.red)`; resume per origin (see resume rules below); return
   - Guard `opts.origin === 'travelMap'` requires `ZoneAccess.status(opts.pendingZone).allowed` — if pendingZone became unavailable, fall back to `gScene('travelMap', true)`
   - `this.buildUI(enc)`
4. `buildUI(enc)`:
   - Header card: `UI.PremiumShell(14, y, G.W-28, 88, ...)` with `enc.icon + '  ' + enc.name` (R.fonts.md, R.colors.accent) and a zone/pool subtitle line (e.g. the origin label) — mirror journeyScene header styling (accent bar via `R.roundRect` on content rect)
   - Prompt block: wrapped via `encounterTextLines(ctx, enc.prompt, G.W-56, R.fonts.md, 6)`; NOTE the journeyScene comment: canvas text does not wrap by itself — use the copy/wrapped lines; draw at increasing line offsets; keep inside the shell
   - Choice cards: one card per `enc.choices[i]`, each `UI.PremiumShell(14, y, G.W-28, cardH, ...)` with custom `render`:
     * Choice index badge (e.g. '1', '2' in a small rounded rect)
     * `choice.text` (R.fonts.md, R.colors.white)
     * `choice.preview` wrapped to 2 lines (R.fonts.sm, R.colors.textSecondary)
     * `btn.enabled = true` — all choices are always selectable this pass (flag-gated choices arrive in 03-03)
     * `btn.onClick = function() { const res = EncounterSystem.choose(encounterScene.data.encounterId, i); if (res) { encounterScene.data.resolved = true; encounterScene.data.result = res; encounterScene.buildResultUI(res); } }`
     * Push to `this.data.buttons`; increment `y` by cardH + 10
   - Set `this.data.contentHeight = y + 24` after building (scroll content band)
   - `this.data.staticDraws = []` — rebuild; register prompt lines as static draws INSIDE the clip/translate so they scroll (scene-helpers convention)
5. `buildResultUI(res)`:
   - Replace buttons with a result panel: `res.text` as heading, `res.granted` lines (one per entry, e.g. '+60 XP', '+3 karma', 'Item: Healing Herb x1', 'Marker set: enc_nagaBargain') each as list rows, then one `UI.MagneticBtn('Continue', ...)` whose onClick resumes per origin rules below
   - `this.data.scrollY = 0`; contentHeight recomputed
6. Resume rules (shared helper `resume()` used by back/continue):
   - `this.data.origin === 'travelMap'`:
     * if `this.data.pendingZone` && `ZoneAccess.status(this.data.pendingZone).allowed` → `ZoneAccess.enter(this.data.pendingZone); gScene('zoneExploration', true)`
     * else → `gScene('travelMap', true)`
   - else (zoneExploration/default) → `gScene('zoneExploration', true)` (zoneExploration.enter reinitializes its timers — the narrative replaced one enemy-encounter slot, acceptable)
7. Back-out behavior (REQ-003 safety + no double-reward): include a small 'Leave' text button (top-right, e.g. `R.text` hit area or MagneticBtn sm) that calls `resume()` UNCHANGED only when `!this.data.resolved` — EncounterSystem.start did NOT mark seen, so backing out leaves zero side effects. After resolve, only 'Continue' exists.
8. `leave: function()` — clear `data.buttons`, `data.staticDraws`, `data.result`, `data.encounterId`; keep nothing stale.
9. `update: function(dt)` — `Scene.scrollInput(this)` when scrolled content exists (buttons.length > 0). No animations; if any micro-animation added later, gate with `R.reducedMotion()`.
10. `render: function(ctx)` — background via existing drawBackground convention (context of the game), then `Scene.drawStatic(ctx, this, ...)` for prompt, then iterate `Scene.cullButtons(this.data.buttons, this.data.scrollY, G.H)` drawing each visible button, then `Scene.drawScrollbar(ctx, topY, this.data.contentHeight, G.H - topY, this.data.scrollY, 24)`.
11. Add `<script src="src/scenes/encounterScene.js"></script>` to `index.html` immediately AFTER `src/scenes/journeyScene.js` (line 124).
Do NOT wire triggers yet (task 2). Do NOT add new globals beyond `encounterScene` and the two local text helpers.
  </action>
  <verify>
    <automated>grep -c "encounterScene" src/scenes/encounterScene.js && grep -c "EncounterSystem.choose" src/scenes/encounterScene.js && grep -c "src/scenes/encounterScene.js" index.html && node -e "const s=require('fs').readFileSync('src/scenes/encounterScene.js','utf8'); if(!s.includes('Scene.create')){process.exit(1)}; if(!s.includes('function resume')){console.error('missing resume helper');process.exit(1)}; if(!s.includes('EncounterSystem.start')){console.error('missing start guard');process.exit(1)}; console.log('scene structure OK')"</automated>
  </verify>
  <done>encounterScene renders prompt + choice cards + result panel; backing out before choosing leaves no markers/ledger writes; resume() returns to the correct origin with pending zone handled; script tag added after journeyScene</done>
</task>

<task type="auto">
  <name>task 2: Wire narrative rolls into zoneExploration and travelMap</name>
  <files>src/scenes/zoneExploration.js, src/scenes/travelMap.js</files>
  <action>
1. `src/scenes/zoneExploration.js` — in `update(dt)`, INSIDE the `encounterTimer <= 0` block (currently at line ~267: pushes 'An enemy appears!' then spawns):
   - BEFORE the enemy spawn path, insert:
     ```js
     const narrId = (typeof EncounterTrigger !== 'undefined' && EncounterTrigger.rollZone) ? EncounterTrigger.rollZone(this.data.zoneId) : null;
     if (narrId) {
       this.data.log.push('Something stirs in the ' + (this.data.zone ? this.data.zone.name : 'wilds') + '...');
       gScene('encounterScene', true, { encounterId: narrId, origin: 'zoneExploration' });
       return;
     }
     ```
   - Keep the existing timer reset (`this.data.encounterTimer = 2 + Math.random() * 2`) in BOTH paths (narrative and enemy), so a narrative encounter consumes one encounter slot and the cadence stays regular. The `return` skips the enemy spawn for the narrative frame.
   - Defensive: never route to `encounterScene` when `narrId` is null — falls through to normal enemy spawn (empty-pool safety).
2. `src/scenes/travelMap.js` — in the travel action (the onClick that currently performs `ZoneAccess.enter(travelMapScene.data.selectedZone)` then `gScene('zoneExploration', true)`, around line 192):
   - Immediately after the zone access check passes (`result.allowed === true`), BEFORE calling `ZoneAccess.enter`, insert:
     ```js
     const narrId = (typeof EncounterTrigger !== 'undefined' && EncounterTrigger.rollTravel) ? EncounterTrigger.rollTravel(G.state.currentZone, travelMapScene.data.selectedZone) : null;
     if (narrId) {
       travelMapScene.data._pendingTravel = travelMapScene.data.selectedZone;
       gScene('encounterScene', true, { encounterId: narrId, origin: 'travelMap', pendingZone: travelMapScene.data.selectedZone });
       return;
     }
     ```
   - On normal path (no narrative): unchanged `ZoneAccess.enter(selectedZone); gScene('zoneExploration', true)`.
   - `leave()` already resets `selectedZone`; also clear `data._pendingTravel = null` in `leave()` and in `enter()` to avoid stale state across visits (matching the travelMap leave/enter hygiene seen at lines 234-236).
   - The boss-fight action (line ~206) and any other actions are untouched.
3. Do NOT modify `EncounterTrigger.NARRATIVE_CHANCE` in scenes — deterministic UAT override happens from the browser console (`EncounterTrigger.NARRATIVE_CHANCE = 1`) during verification only.
  </action>
  <verify>
    <automated>grep -c "EncounterTrigger.rollZone" src/scenes/zoneExploration.js && grep -c "gScene('encounterScene'" src/scenes/zoneExploration.js && grep -c "EncounterTrigger.rollTravel" src/scenes/travelMap.js && grep -c "pendingZone" src/scenes/travelMap.js && node -e "const z=require('fs').readFileSync('src/scenes/zoneExploration.js','utf8'); if(!z.includes('narrId')){console.error('missing narrId guard');process.exit(1)}; const t=require('fs').readFileSync('src/scenes/travelMap.js','utf8'); if(!t.includes('_pendingTravel')){console.error('missing pendingTravel');process.exit(1)}; console.log('trigger wiring OK')"</automated>
  </verify>
  <done>Zone exploration rolls narrative on timer fire before enemy spawn; travel rolls narrative before ZoneAccess.enter with the destination passed as pendingZone; null rolls fall through to normal behavior; pending state cleared on scene enter/leave</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Zone/travel timer → trigger roll | Timer fires frequently; roll must be pure and cheap (no mutation, no unbounded work per frame) |
| encounterScene ← caller payload | opts.encounterId/origin/pendingZone are internal (scene-sourced), but a console call could pass junk — scene must degrade to Notify + resume, never crash |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | encounterScene enter | mitigate | EncounterSystem.start re-validates id/seen; null → Notify + resume to origin (no crash, no reward) |
| T-03-06 | Tampering | choose replay via back-out | mitigate | seen ledger only written in EncounterSystem.choose; back-out path never calls choose (resume() before resolved) |
| T-03-07 | DoS | rolling on every frame | mitigate | rollZone/rollTravel called once per timer fire (2-4s cadence), not per frame; pickers filter + weigh statically |
| T-03-08 | Tampering | stale _pendingTravel | mitigate | cleared in travelMap enter/leave; resume validates ZoneAccess.status before entering zone |
</threat_model>
<verification>
- `grep -c "EncounterTrigger.rollZone" src/scenes/zoneExploration.js` → 1
- `grep -c "gScene('encounterScene'" src/scenes/zoneExploration.js` → 1
- `grep -c "EncounterTrigger.rollTravel" src/scenes/travelMap.js` → 1
- `grep -c "pendingZone" src/scenes/travelMap.js` → >= 2 (payload + resume)
- Node assertions: narrId guard present in zoneExploration; _pendingTravel present in travelMap
- Browser smoke (chrome devtools console via your automation): load save where dandaka is unlocked → `EncounterTrigger.NARRATIVE_CHANCE = 1; EncounterSystem.setFlag('enc_nagaBargain', -1); delete G.state.encounters.seen.nagaBargain` → enter zoneExploration for dandaka → within one timer cycle encounterScene appears for nagaBargain → choose 'Share your gold' → result panel shows granted lines → Continue returns to zoneExploration with exploration running again. Change `EncounterTrigger.NARRATIVE_CHANCE = 0.3` afterwards.
- Back-out path: re-trigger with NARRATIVE_CHANCE = 1, tap 'Leave' before choosing → returns to zoneExploration; `EncounterSystem.isSeen('nagaBargain')` is false; `G.state.flags['enc_nagaBargain']` undefined
</verification>
<success_criteria>
- From dandaka zoneExploration, with the deterministic override, encounterScene reliably appears (NARRATIVE_CHANCE = 1); with normal 0.3 chance it appears occasionally while enemies still spawn on other rolls
- Choosing any card grants rewards, shows the result panel, and Continue returns to the correct origin — travel flow arrives at the pending zone (currentZone = destination, zoneExploration active)
- Backing out before choosing: zero flags, zero ledger writes, zero rewards
- No console errors during either flow; no stale buttons after leave()
</success_criteria>
<output>
After completion, create `.planning/phases/03-mythological-narrative-encounters/03-02-SUMMARY.md`
</output>
