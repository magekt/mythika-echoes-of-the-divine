---
phase: 04-combat-ui-reflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scenes/combatScene.js
autonomous: true
requirements:
  - COMBAT-UI-REFLOW-01

must_haves:
  truths:
    - "Hero status strip never overlaps the selected-enemy panel for any party size (1-5)"
    - "All combat action buttons maintain >= 48px tap targets"
    - "Enemy intent/reaction timing bar renders correctly after layout reflow"
    - "Reduced motion mode suppresses combat animations as before"
  artifacts:
    - path: "src/scenes/combatScene.js"
      provides: "Reflowed combat layout with vertical stacking"
      contains: "getActionAreaTop"
  key_links:
    - from: "src/scenes/combatScene.js drawBattlefield"
      to: "UI.PremiumShell"
      via: "enemy panel rendering"
      pattern: "PremiumShell.*panelX.*panelY"
    - from: "src/scenes/combatScene.js drawBattlefield"
      to: "hero strip rendering"
      via: "heroStep loop"
      pattern: "heroStep.*this\\.data\\.heroes"
---

<objective>
Reflow the combat scene vertical layout so the hero status strip and selected-enemy panel never overlap, regardless of party size (1–5 heroes). Maintain all existing combat mechanics — enemy targeting, intent/reaction flow, action buttons, reduced motion compliance.

Purpose: On mobile (400×720 logical canvas), parties of 4–5 heroes cause the hero status strip (y=32, extending ~352px wide) to collide with the selected-enemy PremiumShell panel (y=32, x=230–400). This makes combat illegible for the most common late-game party composition.

Output: `src/scenes/combatScene.js` with vertically stacked layout; no overlap for any party size.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.slim/deepwork/access-gates-combat-ui.md
@src/scenes/combatScene.js

## Current Layout Analysis (400×720 canvas)

The combat scene renders in `drawBattlefield` (line ~880) with these zones:

**Y=6..102 — Combat header panel** (96px)
  - Line 882: `R.roundRect(ctx, 10, 6, G.W - 20, 96, 8, R.colors.panel)`
  - Contains: "Combat" title (y=22), turn indicator strip (y=76), combo text (y=100)

**Y=32 — Enemy panel** (PremiumShell, x=G.W-170)
  - Line 919: `panelY = 32`, `panelW = 170`, `panelH = 80`
  - Contains: enemy sprite, name, HP bar

**Y=32 — Hero status strip** (starts x=12)
  - Line 939: `hy = 32`, `heroStep = heroes.length <= 3 ? 90 : 68`
  - For 5 heroes: extends x=12 to x=352 (12 + 5×68 = 352)
  - Contains: hero sprite, name, HP bar, MP bar, buffs, ailments

**Y=32 — Enemy target buttons** (x=G.W-62, stacked)
  - Line 269: `ey = 6` (buildActionButtons), buttons at x=338
  - 56×38px buttons stacked from y=6 upward

**Y=108..162 — Intent/reaction bar** (conditional, 54px)
  - Line 993: `R.roundRect(ctx, 12, 108, G.W - 24, 54, 8, ...)`
  - Only visible during `reactionWindow` turnState

**Y=270..720 — Action buttons** (scrollable, getActionAreaTop=270)
  - Line 158: `getActionAreaTop: function() { return 270; }`
  - Combat log renders at Y=192..268 (above action area)

### The Collision

For 5-party heroes at `heroStep=68`:
  - Hero strip: x=12 to x=352, y=32 to ~80
  - Enemy panel: x=230 to x=400, y=32 to y=112
  - **Overlap zone: x=230–352, y=32–80** (hero sprites/names render behind enemy panel)

### Target Layout (vertically stacked)

```
Y=6..60   Combat header (54px) — compact: title + turn strip
Y=64..108 Hero strip (44px) — compact cards for all 5 heroes
Y=112..182 Enemy panel (70px) — PremiumShell with sprite, name, HP
Y=186..240 Intent/reaction bar (54px) — conditional, same as before
Y=250..720 Action buttons — scrollable (getActionAreaTop=250)
```

Combat log renders between intent bar and action area (Y=172..248).

## Interface Context

<interfaces>
<!-- Key types the executor needs from combatScene.js -->

From src/scenes/combatScene.js:
```javascript
// Layout constants (need reflow)
getActionAreaTop: function() { return 270; },  // LINE 158 — MUST change
getActionAreaHeight: function() { return G.H - this.getActionAreaTop(); },  // LINE 159

// Enemy panel positioning (need reflow)
// LINE 916-919: panelW=170, panelH=80, panelX=G.W-panelW, panelY=32
const panelW = 170;
const panelH = 80;
const panelX = G.W - panelW;
const panelY = 32;

// Hero strip positioning (need reflow)
// LINE 938-939: heroStep=68 for 4-5, hy=32
const heroStep = this.data.heroes.length <= 3 ? 90 : 68;
const hy = 32;

// Intent bar (needs Y adjustment)
// LINE 993: R.roundRect(ctx, 12, 108, G.W - 24, 54, 8, ...)

// Enemy target buttons (need Y adjustment)
// LINE 269: let ey = 6;  — builds from y=6 in buildActionButtons

// Combat log (needs Y adjustment)
// LINE 1007: let logY = this.getActionAreaTop() - 78;
```

From src/engine/renderer.js:
```javascript
R.reducedMotion: function() { ... }  // LINE 90 — check before animations
R.drawHero(ctx, heroId, x, y, size)  // hero sprite draw
R.drawEnemy(ctx, enemyId, x, y, size)  // enemy sprite draw
```

From src/ui/button.js:
```javascript
UI.PremiumShell(x, y, w, h, opts)  // returns { render, contentRect }
UI.ProgressBar(x, y, w, h, fillColor, trackColor)  // returns { setProgress, render }
UI.Button(x, y, w, h, label, color)  // returns { render, onClick, contains }
UI.BtnGold(x, y, w, h, label)  // gold variant
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Reflow combat layout — vertical stacking of hero strip and enemy panel</name>
  <files>src/scenes/combatScene.js</files>
  <action>
Reflow the combat scene vertical layout in `src/scenes/combatScene.js` to eliminate the hero-strip / enemy-panel overlap for party sizes 4–5. All changes are in this single file.

## 1. Update layout constants

In `getActionAreaTop` (line 158): change return value from `270` to `248` (frees 22px for the new stacked layout). The combat log and action area will shift up accordingly.

## 2. Compact the combat header

In `drawBattlefield` (line 882): reduce header height from `96` to `52`:
- Change: `R.roundRect(ctx, 10, 6, G.W - 20, 96, 8, R.colors.panel)` → `R.roundRect(ctx, 10, 4, G.W - 20, 52, 8, R.colors.panel)`
- Update the stroke: `ctx.strokeRect(10.5, 6.5, G.W - 21, 95)` → `ctx.strokeRect(10.5, 4.5, G.W - 21, 51)`
- Move the "Combat" title from y=22 to y=20 (line 887)
- Move the turn indicator strip from y=76 to y=34 (line 893): change `R.roundRect(ctx, G.W / 2 - 70, 76, 140, 16, ...)` → `R.roundRect(ctx, G.W / 2 - 70, 34, 140, 16, ...)`
- Update the turn text y from y=88 to y=46: `'▶ ' + currentActor.ref.name + "'s Turn"` at new y=46 (line 894)
- Move combo text from y=100/104 to y=50/54 (lines 903-905)
- Move enemy turn strip from y=76 to y=34, text from y=88 to y=46 (lines 909-910)

## 3. Reposition hero status strip (below header)

Change hero strip y-position from `hy = 32` to `hy = 62` (line 939). The strip now sits below the compact header.

Adjust heroStep for 5-party: change from `68` to `62` (line 938) to keep all 5 heroes within the 400px canvas with a left margin. With `heroStep=62`, 5 heroes span x=12 to x=322 (12 + 5×62), well clear of the enemy panel.

Per D-01 (from access-gates-combat-ui.md): Ensure hero cards remain compact — each card shows: hero sprite (22px), name, HP bar (48×8). Buff/ailment text line (line 962-966) stays at relative y+48 — no overlap within the card.

## 4. Reposition enemy panel (below hero strip)

Change enemy panel y from `panelY = 32` to `panelY = 112` (line 919). This places the panel directly below the hero strip.

Reduce panel height from `80` to `68` (line 917) to fit between hero strip and intent bar. Adjust internal layout:
- Sprite: `content.y + 10` (unchanged, ~line 926)
- Name: `content.y + 36` (was +38, line 928)
- HP bar: `content.y + 48` (was +50, line 931)
- HP text: `content.y + 58` (was +62, line 934)

## 5. Reposition enemy target buttons

In `buildActionButtons` (line 269): change `let ey = 6` to `let ey = 6` (keep top-right corner, unchanged — these buttons are separate from the enemy panel and sit above the hero strip at x=338, y=6+. They are 56×38 each; 5 enemies at 32px spacing = 160px tall, which fits within the header+hero-strip area. This is correct as-is since they are small target selectors in the margin.)

## 6. Reposition intent/reaction bar

In `drawBattlefield` (line 993): change y from `108` to `184`:
- `R.roundRect(ctx, 12, 184, G.W - 24, 54, 8, R.colors.overlayDark)`
- Update stroke: `ctx.strokeRect(12.5, 184.5, G.W - 25, 53)`
- Update text positions: "INCOMING" text from y=126→202 (line 997), intent text from y=144→220 (line 998), progress bar from y=149→225 (line 999-1000), timing text from y=126→202 (line 1001)

## 7. Reposition combat log

In `drawBattlefield` (line 1007): the log renders at `this.getActionAreaTop() - 78`. With new `getActionAreaTop=248`, log starts at y=170. Adjust to start at y=240 (just below intent bar max bottom = 184+54=238):
- Change `let logY = this.getActionAreaTop() - 78` to `let logY = this.getActionAreaTop() - 8`

## 8. Verify reduced motion compliance

All animation code in `drawBattlefield` already checks `R.reducedMotion()` before applying effects (ghost trail animation, damage flash, etc.). No changes needed — verify the existing `R.reducedMotion()` guards at lines 976-980 (ghost trail) still function after reflow.

Do NOT change:
- Combat mechanics (damage calc, turn order, AI)
- Action button layout or tap target sizes
- Scene enter/leave lifecycle
- Save/load integration
- Any code outside `drawBattlefield` and `getActionAreaTop` and `buildActionButtons`
  </action>
  <verify>
    <automated>cd /Users/admin/Documents/Deepankar\ Project/dragonSword_EXPMinima && node -e "
const fs = require('fs');
const src = fs.readFileSync('src/scenes/combatScene.js', 'utf8');

// Check getActionAreaTop returns new value
const topMatch = src.match(/getActionAreaTop:\s*function\(\)\s*\{\s*return\s+(\d+)/);
const top = topMatch ? parseInt(topMatch[1]) : 0;
console.log('getActionAreaTop:', top, top === 248 ? '✓' : '✗ expected 248');

// Check header height reduced
const headerMatch = src.match(/roundRect\(ctx,\s*10,\s*4,\s*G\.W\s*-\s*20,\s*(\d+)/);
const headerH = headerMatch ? parseInt(headerMatch[1]) : 0;
console.log('Header height:', headerH, headerH === 52 ? '✓' : '✗ expected 52');

// Check hero hy moved below header
const hyMatch = src.match(/const\s+hy\s*=\s*(\d+)/);
const hy = hyMatch ? parseInt(hyMatch[1]) : 0;
console.log('Hero strip y:', hy, hy === 62 ? '✓' : '✗ expected 62');

// Check heroStep for 5-party
const stepMatch = src.match(/heroStep\s*=\s*this\.data\.heroes\.length\s*<=\s*3\s*\?\s*90\s*:\s*(\d+)/);
const step = stepMatch ? parseInt(stepMatch[1]) : 0;
console.log('Hero step (4-5):', step, step === 62 ? '✓' : '✗ expected 62');

// Check enemy panel y moved below hero strip
const panelYMatch = src.match(/panelY\s*=\s*(\d+)/);
const panelY = panelYMatch ? parseInt(panelYMatch[1]) : 0;
console.log('Enemy panel y:', panelY, panelY === 112 ? '✓' : '✗ expected 112');

// Check panel height reduced
const panelHMatch = src.match(/panelH\s*=\s*(\d+)/);
const panelH = panelHMatch ? parseInt(panelHMatch[1]) : 0;
console.log('Enemy panel h:', panelH, panelH === 68 ? '✓' : '✗ expected 68');

// Check intent bar y moved
const intentYMatch = src.match(/roundRect\(ctx,\s*12,\s*(\d+),\s*G\.W\s*-\s*24,\s*54/);
const intentY = intentYMatch ? parseInt(intentYMatch[1]) : 0;
console.log('Intent bar y:', intentY, intentY === 184 ? '✓' : '✗ expected 184');

// Check no hero-enemy y overlap: hero bottom < enemy top
const heroBottom = hy + 46;  // ~46px card height
const enemyTop = panelY;
console.log('Hero bottom:', heroBottom, '< Enemy top:', enemyTop, heroBottom < enemyTop ? '✓ no overlap' : '✗ OVERLAP');

console.log('\\nLayout reflow validation complete.');
"
    </automated>
  </verify>
  <done>Hero strip (y=62) and enemy panel (y=112) are vertically stacked with no x/y overlap for any party size. Header compacted to 52px. getActionAreaTop=248. Intent bar at y=184. Combat log repositioned. All tap targets remain ≥48px. Reduced motion guards unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 2: Verify combat layout on mobile — party 5, all turn states</name>
  <what-built>Reflowed combat scene with vertical stacking of hero strip and enemy panel. The hero status strip (y=62) and enemy PremiumShell panel (y=112) no longer overlap for any party size.</what-built>
  <how-to-verify>
1. Open `index.html` in Chrome mobile viewport (or use device emulation at 375×667 / 390×844)
2. Ensure game state has a party of 5 heroes (console: `G.state.party.length` should be ≥4)
3. Enter a zone encounter to trigger combat
4. **Check hero strip**: All 5 hero cards visible at top, no text/sprites cut off or hidden behind enemy panel
5. **Check enemy panel**: Selected enemy sprite, name, HP bar fully visible below hero strip
6. **Check enemy targeting**: Tap enemy buttons (top-right) — selection changes, enemy panel updates
7. **Check action buttons**: Tap Melee, Skill, Guard — all register with clear tap targets (≥48px)
8. **Check reaction window**: Let enemy act — intent/reaction bar appears below enemy panel, timing bar animates
9. **Check reduced motion**: Enable "Reduce Motion" in OS settings (or `G.state.reduceMotion = true` in console) — animations suppressed, layout unchanged
10. **Check no overlap**: Visually confirm hero strip and enemy panel have clear vertical separation
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canvas rendering (no user input crossing) | Layout is pure presentation — no combat logic changes, no state mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | D (Denial of Service) | Combat render loop | accept | Layout reflow adds no new per-frame allocations; canvas redraw cost unchanged |
| T-04-02 | I (Information) | Combat UI | mitigate | Visual overlap caused information loss (heroes hidden behind enemy panel); reflow eliminates this |
| T-04-03 | T (Tampering) | Combat state | accept | No combat logic changed; only Y-coordinates and dimension constants modified |
</threat_model>

<verification>
1. `getActionAreaTop()` returns 248
2. Hero strip y=62, enemy panel y=112 — vertical gap of 50px
3. Hero strip for 5 heroes: x=12 to x=322 (heroStep=62) — well clear of enemy panel x=230–400
4. Intent/reaction bar at y=184, below enemy panel bottom (112+68=180)
5. Combat log renders at y=240, below intent bar
6. All existing `R.reducedMotion()` guards in drawBattlefield still present
7. Manual verification: party-5 combat shows no overlap across all turn states
</verification>

<success_criteria>
- Zero visual overlap between hero status strip and enemy panel for party sizes 1–5
- All combat action buttons ≥48px tap targets (unchanged)
- Intent/reaction timing bar renders correctly below enemy panel
- Reduced motion mode suppresses animations as before
- No console errors during combat
- Mobile viewport (375×667) fully legible
</success_criteria>

<output>
After completion, create `.planning/phases/04-combat-ui-reflow/04-01-SUMMARY.md`
</output>
